# REL-v0.1.0 — AI Context Framework Baseline

## Status

Published retrospectively at tag `v0.1.0`, commit `69c285077708dfb96ee49bb39258aec83eb7f1a9`.

This record was created after the tag to establish the first comparable framework baseline. It does not imply that release governance artifacts existed at publication time.

This tag is a `source-snapshot-only` provenance anchor and is not an installable package. Its repository tree contains source lifecycle truth that must not be raw-copied into a target.

## Included Baseline

- Reusable AI collaboration context and .NET backend guidance.
- Canonical skill assets with Codex and Claude runtime surfaces.
- Repository workflow, assessment, Git, and AI-context governance accumulated through the baseline commit.

## Compatibility

This is the historical baseline. No earlier governed version is available for compatibility comparison.

## Known Limitations

- No target provenance manifest contract.
- No canonical release or upgrade workflow.
- Consumers must identify this snapshot by both tag and commit.
- New installations must use a published governed package rather than this tag tree.
