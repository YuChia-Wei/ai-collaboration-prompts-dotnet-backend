# REL-v0.0.1 — Early AI Context Source Snapshot

## Status

Published retrospectively at annotated tag `v0.0.1`, commit
`ac2e2937b5209ece93e104c4a389a15e164c0d1b`.

This record was created after the historical source was used. It establishes a
stable comparison identity; it does not imply that release governance,
distribution packaging, or target provenance existed at that time.

This tag is `source-snapshot-only` and is not an installable package. Do not
copy its complete repository tree into a target.

## Included Historical State

- Early portable documentation and repository-structure synchronization work.
- Pre-governance AI collaboration context and .NET backend guidance.
- No `ai-context-governance` skill, governed package envelope, target provenance manifest, or version-aware upgrader.

## Compatibility

There is no earlier governed version. A target believed to derive from this
snapshot may use the tag as a candidate three-way comparison base only after
its installed file selection and local changes are inventoried.

## Known Limitations

- No release package, package inventory, or previous-file hash baseline.
- No exact record of which files each historical target copied.
- No automatic upgrade path; later versions require unresolved-provenance and manual reconciliation.
- Root entries, project knowledge, workflows, and local context may already have diverged substantially.
