schema_version: "1.0"
activation_fixture: ".ai/assets/skills/dev-workflow/fixtures/acceptance/activation-high-level-pending-approval.yaml"
test_execution:
  selected_levels:
    - "unit"
    - "integration"
  required_for_closeout:
    - "unit"
    - "integration"
  outcomes:
    unit:
      status: "passed"
      evidence:
        - "unit command exited 0"
    integration:
      status: "blocked"
      evidence:
        - "target environment authorization is unavailable"
